name: {{cookiecutter.project_name}}
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - pytest
  - jupyterlab
  - pip
  - pip:
      - ipython
      - ipykernel # ipython
      - nb-black  # black codeformatter in notebooks
      # docs
      - mkdocs
      - mkdocs-material
      - pygments
      - mkdocs-minify-plugin
      - pymdown-extensions
      # packaging
      - semantic-version